import { GrandLayout } from "./grand/GrandLayout";

/**
 * Single design: "Midnight Atelier". The previous light/dark layout lives
 * on the `classic-theme` branch if it is ever needed again.
 */
export default function App() {
  return <GrandLayout />;
}
